[project]
name = "caddy-log-processor"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "aiogram>=3.24.0",
    "anyio>=4.12.0",
    "trio>=0.32.0",
]
